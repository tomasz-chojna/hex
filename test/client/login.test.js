import { expect } from 'chai';
import {LoginComponent} from './../../src/client/components/login';

describe('LoginComponent', () => {

    it('should render proper state', () => {
        expect(true).to.equal(true);
    });

});
